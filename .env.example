# Sales Prep Agent Configuration
# Copy this file to .env and fill in values

# Toggle mock data (set to "false" when MCP servers are configured)
USE_MOCK_DATA=true

# GitHub Copilot SDK (ensure `gh auth login` is complete and Copilot CLI is installed)
# No explicit token needed — Copilot SDK uses `gh` CLI auth automatically.

# Work IQ MCP — Microsoft Graph via @microsoft/workiq
# WORKIQ_TENANT_ID=<your-entra-tenant-id>

# Fabric IQ MCP — Microsoft Fabric via @microsoft/fabric-mcp
# FABRIC_WORKSPACE_ID=<your-fabric-workspace-id>

# Foundry IQ MCP — Azure AI Search knowledge base
# AZURE_SEARCH_ENDPOINT=https://<name>.search.windows.net
# AZURE_SEARCH_KEY=<your-search-api-key>
# AZURE_SEARCH_INDEX=sales-enablement

# Output directory for generated documents
OUTPUT_DIR=output
